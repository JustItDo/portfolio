# Klucz API Resend (https://resend.com) do wysyłki formularza kontaktowego.
# Bez klucza endpoint /api/contact zwraca 503, a UI pokazuje fallback mailto.
# Produkcyjnie: plik .env na serwerze (nigdy w repo).
RESEND_API_KEY=
